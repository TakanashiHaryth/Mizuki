import { createSocialDownloadHandler } from './socialDownload';

/** Downloads a public Instagram post or its audio track. */
export const instagramDownloadHandler = createSocialDownloadHandler('ig', 'Instagram');
